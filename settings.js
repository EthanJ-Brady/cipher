class Settings {
    constructor() {
        const seed = document.getElementById("seed");
        const bidirectionalSwitch = document.getElementById("bidirectional-cards-switch").querySelector("input");
        const teamForm = document.getElementById("team-count");
    }
}